#!/usr/bin/perl
# This file is 'required' by the main startup script, it's never run directly.

# PROD 

our $HTTP_PORT= 			27710;
our $JETTY_TMP_DIR= 		'/opt/data/hawthorne-server/jetty_tmp';
our $JAVA_HOME=             '/usr/lib/jvm/java-1.6.0-sun.x86_64'; 
1;
