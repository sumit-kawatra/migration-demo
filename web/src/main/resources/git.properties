# This file is automatically updated by maven when copying resources.
# Provides the code with build-level information.  Currently only works in Jenkins,
# when running the 'runInJenkins' profile.
build.git.sha=${buildNumber}